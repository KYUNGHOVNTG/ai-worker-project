---
name: sdd
description: "SDD(Schema-Driven Development) 워크플로우 시작. 대화형 질문으로 요구사항을 수집하고 구현 로드맵을 생성한다."
---

# SDD (Schema-Driven Development) — 로드맵 생성

당신은 SDD 워크플로우 진행자입니다.
사용자로부터 **단계별로 하나씩** 정보를 수집한 뒤, 구현 로드맵 MD 파일을 생성합니다.

## 인자

- `$ARGUMENTS` — 메뉴명 (선택). 예: `/sdd 대상자관리`

---

## 1단계: 정보 수집 (대화형)

아래 5가지 항목을 **하나씩 순서대로** 질문합니다.
사용자가 답변하면 다음 질문으로 넘어갑니다.
**절대 한 번에 여러 질문을 하지 마세요.**

만약 `$ARGUMENTS`로 메뉴명이 제공되었으면 "메뉴명: {$ARGUMENTS}로 진행합니다"를 먼저 확인하고, 질문 1은 건너뜁니다.

### 질문 순서

**질문 1 — 메뉴명**
> "구현할 메뉴(화면)의 이름을 알려주세요."
> 예시: 대상자관리, 급여명세서, 근태현황

**질문 2 — 테이블 + DDL**
> "관련 테이블명과 DDL(또는 주요 컬럼 목록)을 알려주세요."
> "CREATE TABLE 문, 컬럼명+타입 목록, 또는 ERD 캡처 중 편한 형태로 주세요."
> 예시:
> ```
> HR95200 (업적평가대상자): EVAL_YEAR, EMP_NO, EVAL_TYPE, ASSESSOR_1, ASSESSOR_2, ...
> HR95600 (평가마감관리): EVAL_YEAR, EVAL_TYPE, CLOSE_YN
> ```

**질문 3 — 라우터 URL**
> "API 경로와 프론트엔드 경로를 알려주세요."
> 예시:
> - API: `/api/v1/eval/targets`
> - 프론트: `/app/admin/eval/trgt-mgnt`

**질문 4 — 요구사항**
> "주요 기능 명세를 알려주세요. 가능한 구체적으로 작성해주시면 정확한 태스크 분리가 가능합니다."
> 다음 항목들을 포함하면 좋습니다:
> - UI/UX 구조 (탭, 테이블, 폼 등)
> - 핵심 비즈니스 로직
> - 조회/생성/수정/삭제 범위
> - 권한/상태 제어 (마감 처리 등)
> - 단계적 구현 범위 (이번 Phase에서 할 것 / 안 할 것)

**질문 5 — 레거시 참조 (선택)**
> "참조할 레거시 함수, 프로시저, 쿼리가 있으면 알려주세요. 없으면 '없음'이라고 답해주세요."
> 예시: SF_IN_PART_LEADER_BY_DEPT(부서별 파트장 조회), 기존 화면 캡처 등

---

## 2단계: 수집 내용 확인

모든 정보를 수집한 후, 수집된 내용을 아래 형식으로 **요약 정리하여 사용자에게 확인**을 받습니다:

```
## 수집 완료 — 확인 부탁드립니다

| 항목 | 내용 |
|------|------|
| 메뉴명 | {메뉴명} |
| 테이블 | {테이블 목록 요약} |
| API 경로 | {API URL} |
| 프론트 경로 | {프론트 URL} |
| 핵심 요구사항 | {1~3줄 요약} |
| 레거시 참조 | {있음/없음 + 목록} |

이 내용으로 로드맵을 생성할까요?
```

사용자가 승인하면 3단계로 진행합니다. 수정 요청이 있으면 반영 후 다시 확인합니다.

---

## 3단계: 로드맵 MD 생성

### 생성 전 필수 작업

1. **프로젝트 현황 분석** — 현재 코드베이스에서 이미 존재하는 관련 코드를 확인합니다:
   - `server/app/domain/` 하위 기존 도메인
   - `client/src/domains/` 하위 기존 도메인
   - `alembic/versions/` 기존 마이그레이션
   - `server/app/api/v1/router.py` 기존 라우터 등록 현황

2. **sample 도메인 참조** — 태스크의 파일 구조는 아래 예시를 따릅니다:
   - 백엔드: `server/app/examples/sample_domain/` (models, schemas, repositories, calculators, formatters, service)
   - 프론트: `client/src/domains/sample/` (types, api, store, components, pages)

### 로드맵 작성 규칙

**파일 경로**: `docs/roadmaps/YYYY-MM-DD-roadmap-{name}.md`

**로드맵 구조**:

```markdown
# {메뉴명} 구현 로드맵

> 작성일: YYYY-MM-DD
> 메뉴명: {메뉴명}
> 프론트 경로: {URL}
> API 경로: {URL}

---

## 수집된 요구사항

{2단계에서 확인받은 전체 요구사항 기록}

---

## 현재 코드베이스 상태

| 영역 | 상태 |
|------|------|
| ... | 있음/없음 |

---

## DB 마이그레이션 필요 항목

| 테이블 | 변경 내용 | 마이그레이션 파일명 |
|--------|----------|-------------------|
| {테이블명} | {변경 설명} | YYYY-MM-DD-{description} |

---

## 전체 Task 요약 및 실행 순서

| # | Task | 설명 | 추천 모델 | 상태 |
|---|------|------|----------|------|
| 1 | Task 1 | ... | Opus 4.6 / Sonnet 4.6 | ⬜ |
| 2 | Task 2 | ... | ... | ⬜ |

상태: ⬜ 대기 / 🔄 진행중 / ✅ 완료

---

## Task 1: {제목}

**추천 모델**: `claude-sonnet-4-6` 또는 `claude-opus-4-6`
> {왜 이 모델인지 1줄 설명}

**작업 목표**
{1줄 설명}

**작업 범위**

| 작업 | 파일 경로 |
|------|-----------|
| 생성 | `server/app/domain/{name}/models/__init__.py` |
| 수정 | `server/app/api/v1/router.py` |
| ... | ... |

**완료 기준**
- [ ] {구체적 완료 조건 1}
- [ ] {구체적 완료 조건 2}

**주의사항**
- {있을 경우}

**검수 체크리스트**
- [ ] 레이어드 아키텍처 준수 (Router → Service → Repository)
- [ ] 타입 힌트 / TypeScript 타입 누락 없음
- [ ] (백엔드) Alembic 마이그레이션 파일 생성 확인
- [ ] (백엔드) `api/v1/router.py`에 라우터 등록 확인
- [ ] (프론트) `apiClient` 사용 확인 (직접 axios 금지)
- [ ] (프론트) `App.tsx`에 라우트 등록 확인
- [ ] 기존 파일의 의도치 않은 변경 없음

### 완료 기록 (태스크 완료 후 작성)

- **완료일**: -
- **변경 파일**: -
- **커밋**: -
- **다음 태스크 참고사항**: -
- **미해결 이슈**: -

---

(Task 2, 3, ... 동일 형식으로 반복)
```

### Task 분리 기준

- **하나의 Task = 한 번의 Claude Code 세션에서 완결 가능한 단위**
- Task 간 의존 관계가 있으면 순서 명시
- 일반적인 분리 패턴:
  1. DB 모델 + 마이그레이션 (항상 첫 번째)
  2. 백엔드 Repository + Service
  3. 백엔드 Router (API 엔드포인트)
  4. 프론트엔드 타입 + API 모듈
  5. 프론트엔드 Store + Components + Pages
  6. 프론트엔드 라우트 등록 + 통합

### 모델 추천 기준

| 작업 유형 | 추천 모델 | 이유 |
|----------|----------|------|
| DB 모델/마이그레이션 | Sonnet 4.6 | 구조적, 패턴 반복 |
| 단순 CRUD Repository/Service | Sonnet 4.6 | 보일러플레이트 성격 |
| 복잡한 비즈니스 로직 | Opus 4.6 | 추론/설계 판단 필요 |
| 프론트 컴포넌트/페이지 | Sonnet 4.6 | UI 패턴 반복 |
| 복잡한 상태 관리/연동 | Opus 4.6 | 여러 레이어 동시 고려 |

---

## 4단계: 자체 점검

로드맵 초안 작성 후 아래를 자체 점검하고 수정합니다:
- Task 간 의존관계 누락 여부
- 파일 경로 정확성 (실제 프로젝트 구조와 일치하는지)
- DB 마이그레이션 필요한데 누락된 항목
- 완료 기준이 모호한 항목

---

## 5단계: 저장 및 안내

로드맵 MD 파일을 저장한 후 사용자에게 안내합니다:

```
로드맵이 생성되었습니다: docs/roadmaps/YYYY-MM-DD-roadmap-{name}.md

다음 단계:
1. 로드맵 내용을 검토해주세요
2. 수정이 필요하면 말씀해주세요
3. 작업을 시작하려면 `/sdd-task` 를 실행해주세요
```
